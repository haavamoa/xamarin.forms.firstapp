<?xml version="1.0"
      encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MyFirstApp"
             x:Class="MyFirstApp.Views.MainPage"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:ViewModels="clr-namespace:MyFirstApp.ViewModels;assembly=MyFirstApp"
             d:DataContext="{d:DesignInstance ViewModels:MainViewModel}" >

    <StackLayout>
        <!-- SOFIE ER KUL-->
        <Label Text="{Binding YourAge}" />
        <Slider Value="{Binding YourAge}" Minimum="0" Maximum="1000" />
        <Label Text="{Binding Text}" />
    </StackLayout>

</ContentPage>